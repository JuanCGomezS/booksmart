---
export function getStaticPaths() {
  return [{ params: { slug: undefined } }];
}

import Layout from '../../layouts/Layout.astro';
import BarberApp from '../../components/react/barber/BarberApp';
---

<Layout title="Reservas" description="Reserva tu cita directamente con este negocio." autonomous>
  <BarberApp client:only="react" />
</Layout>
